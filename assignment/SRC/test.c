
#include "bitmask.h"
#include "mystring.h"
#include "myutils.h"

#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int main() {
 char str_input1[50]="HELLO";
 char str_input2[50];
 int length,set_val,reset_val,setans,resetans,fact;
 char answer[10];
  set_val = 0;
  reset_val = 1;
  length = strlen(str_input1);
  printf("Length: %d", length);
answer[10] = strcmp(str_input1,str_input2);
  printf("Result: %s", answer);
  //fact = find_factorial(4);
//setans = myset(set_val);
//resetans = myreset(reset_val);
 char s1[20]="Hello";
 char s2[20]="World";
printf("%s",strcat(s1[20],s2[20]));
  return 0;
}