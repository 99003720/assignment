#include "myutils.h"


error_o find_factorial(int num, long* result)
{
   // long temp_result = 1;
    if(1 == num)
    {
        return num;int fact=0;
int fact_val = 4;
long result = 1;
fact = find_factorial(fact_val,&result);
    {
        return 0;
    }
    else
    {
        while (1 <= num)
        {
            *result *= num--;
        }
        return result;
    }
    
}